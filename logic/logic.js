function treeFunc(start, level, divider) {
  let arrTree = [];
  let arrInside = [];
  for (var i = 0; i < level; i++) {
    // arrInside.push(start);
    // // for (var i = 0; i < array.length; i++) {
    // //   array[i]
    // // }
    // arrTree.push(arrInside.slice())
    
  }
  console.log(arrTree);
}

treeFunc(3, 4);
